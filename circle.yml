machine:
  environment:
    IMPORT_PATH: "github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"

dependencies:
  pre:
    - wget "https://github.com/Masterminds/glide/releases/download/0.8.2/glide-0.8.2-linux-amd64.tar.gz"
    - tar -vxz -C $GOPATH/bin --strip=1 -f glide-0.8.2-linux-amd64.tar.gz
    - mkdir -p "$GOPATH/src/$IMPORT_PATH"
    - rsync -azC --delete ./ "$GOPATH/src/$IMPORT_PATH/"
  override:
    - make bootstrap build:
        pwd: "$GOPATH/src/$IMPORT_PATH/"
  cache_directories:
    - vendor

test:
  override:
    - make test lint

# Disabled for now to avoid conflicts with the Jenkins builds:

#deployment:
#  hub:
#    branch: master
#    owner: fabric8io
#    commands:
#      - docker build -t fabric8/kansible .
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push fabric8/kansible
